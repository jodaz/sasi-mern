import React from 'react';
import { Helmet } from 'react-helmet';

const Login = () => (
  <React.Fragment>
    <Helmet>
      <title>Login | SASI</title>
    </Helmet>
    <div>
      You are in login page
    </div>
  </React.Fragment>
);

export default Login;